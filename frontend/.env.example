# Copie para frontend/.env.local e preencha se APP_AUTH_TOKEN estiver definido no .env da raiz.
# Deve ser o MESMO valor de APP_AUTH_TOKEN (usado pelo backend).
VITE_APP_AUTH_TOKEN=
VITE_MAX_AVATAR_BYTES=1073741824
